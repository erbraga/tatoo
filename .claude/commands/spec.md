---
description: Explora o projeto e escreve uma spec de feature seguindo o template do repositório
argument-hint: [nome da feature]
---

Leia `CLAUDE.md` para entender as convenções do projeto.

Explore os arquivos relevantes (models, views, urls, templates) relacionados a "$ARGUMENTS" antes de propor qualquer coisa — não assuma estrutura sem confirmar lendo o código.

Copie `docs/specs/TEMPLATE.md` para `docs/specs/AAAA-MM-DD-$ARGUMENTS.md` (use a data de hoje) e preencha cada seção com base no que você encontrou no código e no que eu descrever a seguir sobre a feature.

Regras:
- Não escreva código nesta etapa.
- Se algo for ambíguo ou exigir uma decisão de produto (ex: qual gateway de pagamento usar), liste em "Decisões em aberto" em vez de assumir.
- Ao terminar, pare e me mostre a spec para revisão. Não siga para o plano sem eu confirmar.
